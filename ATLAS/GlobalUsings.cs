// Project-wide global usings. System.IO is not part of the default implicit using set for this
// SDK/target, so it is added here (Path, File, Directory are used across services).
global using System.IO;
