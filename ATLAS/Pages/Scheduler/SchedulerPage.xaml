<UserControl x:Class="Atlas.Pages.Scheduler.SchedulerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Atlas.Brush.Background}">
    <UserControl.Resources>
        <Style x:Key="Tool" TargetType="Button" BasedOn="{StaticResource Atlas.Button.Secondary}">
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- No-active-profile overlay -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding HasActiveProfile, Converter={StaticResource BoolToVisibility}, ConverterParameter=Invert}">
            <TextBlock Text="No active profile" FontSize="22" FontWeight="SemiBold"
                       HorizontalAlignment="Center" Foreground="{DynamicResource Atlas.Brush.TextPrimary}"/>
            <TextBlock Text="Activate a profile to manage its scheduled tasks."
                       Margin="0,10,0,0" HorizontalAlignment="Center"
                       Foreground="{DynamicResource Atlas.Brush.TextSecondary}"/>
        </StackPanel>

        <DockPanel Margin="16"
                   Visibility="{Binding HasActiveProfile, Converter={StaticResource BoolToVisibility}}">

            <TextBlock DockPanel.Dock="Top" Text="Scheduler" FontSize="22" FontWeight="SemiBold"
                       Margin="0,0,0,12" Foreground="{DynamicResource Atlas.Brush.TextPrimary}"/>

            <!-- Toolbar -->
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Style="{StaticResource Atlas.Button.Primary}" Content="Add" Padding="14,6" Margin="0,0,6,0"
                        Command="{Binding AddCommand}"/>
                <Button Style="{StaticResource Tool}" Content="Edit" Command="{Binding EditCommand}"/>
                <Button Style="{StaticResource Tool}" Content="Clone" Command="{Binding CloneCommand}"/>
                <Button Style="{StaticResource Tool}" Content="Delete" Command="{Binding DeleteCommand}"/>
                <Button Style="{StaticResource Tool}" Content="Run Now" Command="{Binding RunNowCommand}"/>
                <Button Style="{StaticResource Tool}" Content="Enable / Disable" Command="{Binding ToggleEnabledCommand}"/>
                <Button Style="{StaticResource Tool}" Content="Refresh" Command="{Binding RefreshCommand}"/>
            </StackPanel>

            <!-- Status footer -->
            <Border DockPanel.Dock="Bottom" Margin="0,10,0,0" Padding="8,6"
                    Background="{DynamicResource Atlas.Brush.Surface}"
                    BorderBrush="{DynamicResource Atlas.Brush.Border}" BorderThickness="1" CornerRadius="4">
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource Atlas.Brush.TextPrimary}"/>
            </Border>

            <!-- Task grid -->
            <Border Background="{DynamicResource Atlas.Brush.Surface}"
                    BorderBrush="{DynamicResource Atlas.Brush.Border}" BorderThickness="1" CornerRadius="4">
                <DataGrid ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}"
                          AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column"
                          IsReadOnly="True" GridLinesVisibility="Horizontal"
                          Background="Transparent" BorderThickness="0"
                          RowBackground="{DynamicResource Atlas.Brush.Surface}"
                          Foreground="{DynamicResource Atlas.Brush.TextPrimary}"
                          HorizontalGridLinesBrush="{DynamicResource Atlas.Brush.Border}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding TypeText}" Width="110"/>
                        <DataGridTextColumn Header="Cron" Binding="{Binding Cron}" Width="120"/>
                        <DataGridTextColumn Header="Schedule" Binding="{Binding HumanCron}" Width="170"/>
                        <DataGridTextColumn Header="Next Run" Binding="{Binding NextRunText}" Width="140"/>
                        <DataGridTextColumn Header="Last Run" Binding="{Binding LastRunText}" Width="140"/>
                        <DataGridTextColumn Header="Result" Binding="{Binding LastRunResult}" Width="2*"/>
                        <DataGridTextColumn Header="State" Binding="{Binding EnabledText}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>
